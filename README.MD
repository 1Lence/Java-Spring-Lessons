Петров Кирилл Антонович

ДЗ по Application events and listeners:

● Создал три EventListener’a (обычный, Async и Transactional)
● Для каждого из них порождается событие
● Для TransactionalEventListener порождается два события так, чтобы одно
обрабатывалось, а другое нет